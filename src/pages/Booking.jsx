import BookingDetail from "../features/bookings/BookingDetail.jsx";

// Main component for rendering the Booking page without directly fetching data.
function Booking() {
  return <BookingDetail />;
}

export default Booking;
