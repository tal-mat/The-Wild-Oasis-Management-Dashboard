import styled from "styled-components";

// Flex container for table actions with centered alignment and spacing
const TableOperations = styled.div`
  display: flex;
  align-items: center;
  gap: 1.6rem;
`;

export default TableOperations;
